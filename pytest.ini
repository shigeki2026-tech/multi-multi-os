[pytest]
# Collect tests only from the tests/ directory.
# (The repo root contains test_db.py saved as UTF-16, which breaks pytest
#  collection with a "null bytes" error, so we limit discovery to tests/.)
testpaths = tests
